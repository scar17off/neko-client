{
  "name": "neko-client",
  "version": "0.1.0",
  "description": "A client for the ourworldofpixels website",
  "scripts": {
    "start": "",
    "build": "npx esbuild src/js/main.js --bundle --outdir=build --loader:.mp3=dataurl --loader:.png=dataurl && npx cpx \"src/**/*.{html,css,ico,woff,woff2,png,gif,mp3}\" build && npx cpx -r src/packages build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/"
  },
  "keywords": [],
  "author": "",
  "license": "The Unlicense",
  "bugs": {
    "url": "https://github.com/"
  },
  "homepage": "https://github.com/",
  "devDependencies": {
    "cpx": "^1.5.0",
    "esbuild": "^0.25.8"
  },
  "dependencies": {
    "events": "^3.3.0"
  }
}
